name: my-strapi-app
version: 1
environments:
  production:
    commands:
      # Use npm i instead of npm ci
      build: npm install && npm run build
      start: npm start
addons:
  # Add MongoDB addon
  - mongodb
